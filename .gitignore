# Build artifacts
dist/
logs/
# macOS
.DS_Store
**/.DS_Store
Test-SecondBrain/
*.zip
# Dev config
.claude/settings.local.json
.mcp.json
# GSD planning artifacts (internal workflow — not for upstream)
.planning/
# Personal Claude Code permissions cache (not for fork)
.claude/settings.json

# Secrets — defense in depth (don't commit these even if accidentally created)
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
*_rsa
*_ed25519
*.cert
# Local databases
*.sqlite
*.sqlite3
*.db
# IDE state
.vscode/
.idea/
*.swp
*.swo
